{{- if .Values.mysql.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "cbctf.mysql.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cbctf.mysql.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.mysql.service.port }}
      targetPort: mysql
      protocol: TCP
      name: mysql
  selector:
    {{- include "cbctf.mysql.selectorLabels" . | nindent 4 }}
{{- end }}
